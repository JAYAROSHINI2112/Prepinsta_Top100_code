
#include <stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    int n,i,j = 0,sum=0,val;
    char str[20],l;
    scanf("%s",&str);
    
    for(i = 0; str[i] != '\0'; i++)
    str[j] = toupper(str[j]);
    str[i - 1] = toupper(str[i - 1]);
    printf("%s",str);
}
